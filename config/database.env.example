# Copy to config/database.env. Do not commit the real file.
# Use a unique password and an isolated database for every development instance.
POSTGRES_DB=pv_vault
POSTGRES_USER=pv_vault
POSTGRES_PASSWORD=replace-with-a-long-unique-database-password
